# Qodana configuration:
# https://www.jetbrains.com/help/qodana/qodana-yaml.html

version: "1.0"
# Pin Qodana linter to a specific version to ensure reproducible analysis
linter: jetbrains/qodana-jvm-community:2025.2.1
projectJDK: "21"
profile:
  name: qodana.recommended

exclude:
  - name: All
    paths:
      - .qodana
      - .gradle
      - build
      - docker
