# Console runtime config (read by env_file)

# Downstream HTTP endpoints (plugin on host, browser connects to localhost)
HTTP_1=http://localhost:8888
HTTP_2=http://localhost:8889
HTTP_3=http://localhost:8890

# Downstream WebSocket endpoints (plugin on host, browser connects to localhost)
WS_1=ws://localhost:8891
WS_2=ws://localhost:8892
WS_3=ws://localhost:8893

# Console WS token
WS_TOKEN=zhongmiao-org-token
